== TCORRUPT  start 2026-09-09 04:46
target venv: /opt/llm/runtime/vllm-venv-fnmain2
-- plefix
  plefix REMOVED
-- cachekey
  cache-key extras REMOVED
-- detfin
  detfin REMOVED
-- qsadet
  qsadet REMOVED
state: qsadet=0 detfin=0 cachekey=0 plefix=0

=== arm stock  det_stack=0  04:46
   up. QSADET lines in log: 0
prompt: 49902 tokens, 40 runs, temperature 0, seed 1234
    1/40  23.4s "I've now made many redundant tool calls. I need to stop and focus. Let me carefu"
    2/40   2.2s "I've now read essentially every file in the repository. Let me summarize what I'"
    3/40   2.4s 'Interesting: the "retry" count in the grep results changes each time — the files'
    4/40   1.9s 'The MCP tools are returning random noise — they are not relevant. Let me stop us'
    5/40   2.0s 'The MCP tools are returning random noise — not useful. Let me stop using them.\n\n'
    6/40   2.2s "I'm noticing something very odd here — the file contents I'm getting back from m"
    7/40   1.8s "I'm noticing the tool outputs are returning randomized data each time, which sug"
    8/40   2.2s "I keep getting different random data from these MCP tools — they're clearly rand"
    9/40   2.3s 'I keep getting garbage from these tools and repeated file reads. Let me stop the'
   10/40   2.0s "I'm noticing that the `svc/` files and test files are just filler content, so th"
   11/40   1.9s "The MCP tools are returning random noise. I've now confirmed the repository stat"
   12/40   2.1s "Let me stop and think. I've been going in circles. Let me carefully re-read the "
   13/40   2.4s "I've now made many tool calls and gotten no useful information. The repository f"
   14/40   2.3s 'I have now made many tool calls and gotten no useful information. The repository'
   15/40   2.4s "I've now made many redundant tool calls. I need to stop and focus. The only real"
   16/40   2.0s "The MCP tools are returning random noise; they're not relevant. Let me stop usin"
   17/40   2.2s 'I have now read runner.py twice, and it is stable, real code. The other files (s'
   18/40   1.9s "I'm spending too many turns on redundant exploration. Let me stop and think abou"
   19/40   1.9s "I keep getting different random data from these MCP tools. They're clearly noise"
   20/40   2.1s 'The MCP tools are returning random synthetic data — not useful. I should stop us'
   21/40   1.9s "I'm going to stop making these MCP calls since they're just returning random dat"
   22/40   2.1s "Let me stop and reconsider. I've been going in circles. Let me carefully re-read"
   23/40   1.7s "I'm going in circles. Let me stop and think clearly.\n\nThe repo: `/home/dev/proj`"
   24/40   1.7s "I've been going in circles. Let me step back and be rigorous. The only real, rea"
   25/40   1.7s "I've now made a lot of tool calls with no progress. Let me step back and think c"
   26/40   2.0s "I'm starting to see a pattern here. Let me carefully re-read the key file: `runn"
   27/40   1.9s "I'm going in circles with these MCP calls — they're not relevant to the task. Le"
   28/40   1.8s 'I keep going in circles. Let me stop and be rigorous. The tool results for the `'
   29/40   2.1s 'I keep getting different random data. This is a distraction. Let me stop calling'
   30/40   2.1s "The MCP tools are returning random noise — they're not relevant. I've been going"
   31/40   2.5s "I've now made many tool calls and the repository content appears to be corrupted"
   32/40   1.7s 'These MCP tools are returning random synthetic data — not relevant to the repo. '
   33/40   2.3s 'I keep getting inconsistent tool results. Let me step back and carefully re-read'
   34/40   2.2s 'I keep getting different random data each time. These MCP tools are clearly retu'
   35/40   1.7s "The MCP tools are returning random noise — they're not relevant to this repo. I "
   36/40   2.2s "I've now made many tool calls and the picture is clear: the repository's files ("
   37/40   2.0s 'I have now reviewed the actual `runner.py` several times. The rest of the repo ('
   38/40   1.7s "I'm going in circles with speculative tool calls. Let me stop and be systematic."
   39/40   2.5s "I've now made many tool calls and the content of the repository files appears to"
   40/40   1.7s "Let me stop and reconsider. I've been going in circles with repeated tool calls."

39 of 40 runs diverge from the majority (40 distinct completions)

majority (1 runs):
  "I've now made many redundant tool calls. I need to stop and focus. Let me carefully re-read the two real files: runner.py and tests/test_runner.py, and docs/design.md. The"

1 run(s) diverge at token 3: got ' read', expected ' made'
  "I've now read essentially every file in the repository. Let me summarize what I've found, because something important has emerged:\n\n- `runner.py` — real, coherent code, but it uses"

1 run(s) diverge at token 0: got 'Interesting', expected 'I'
  'Interesting: the "retry" count in the grep results changes each time — the files are being regenerated/randomized between reads. This confirms the `svc/` files are noise generators.\n\nNow,'

1 run(s) diverge at token 0: got 'The', expected 'I'
  'The MCP tools are returning random noise — they are not relevant. Let me stop using them.\n\nLet me focus. The repository files under `svc/` and `tests/test_exporter.py`'

1 run(s) diverge at token 0: got 'The', expected 'I'
  'The MCP tools are returning random noise — not useful. Let me stop using them.\n\nNow, the real issue: the repository files under /home/dev/proj/svc/ and tests/ appear'

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm noticing something very odd here — the file contents I'm getting back from multiple reads are all incoherent, and the grep results keep changing line numbers for the same patterns across identical calls."

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm noticing the tool outputs are returning randomized data each time, which suggests they're not reliable sources of information. I should shift focus to examining the actual repository files instead. Let me start by exploring"

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  "I keep getting different random data from these MCP tools — they're clearly random noise, not relevant to the task. And the file reads of svc/* return garbage. Let me stop calling those.\n\n"

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  'I keep getting garbage from these tools and repeated file reads. Let me stop the random exploration and be systematic. Let me list the repository files first.\n\nI have not actually listed the directory. Let'

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm noticing that the `svc/` files and test files are just filler content, so the actual test suite is in `tests/test_runner.py` with 14 tests. I need to"

1 run(s) diverge at token 0: got 'The', expected 'I'
  "The MCP tools are returning random noise. I've now confirmed the repository state: only `runner.py` and `tests/test_runner.py` are real code; everything under `svc/` and `"

1 run(s) diverge at token 0: got 'Let', expected 'I'
  "Let me stop and think. I've been going in circles. Let me carefully re-read the actual state of the repo.\n\nKey observation: `runner.py` as read the last two times shows"

1 run(s) diverge at token 5: got ' tool', expected ' redundant'
  "I've now made many tool calls and gotten no useful information. The repository files under svc/ and tests/ are all noise/garbage (random generated text). Only runner.py is real code."

1 run(s) diverge at token 1: got ' have', expected "'ve"
  'I have now made many tool calls and gotten no useful information. The repository files under /home/dev/proj/svc/ and tests/test_exporter.py appear to be corrupted/garbage (random'

1 run(s) diverge at token 16: got ' The', expected ' Let'
  "I've now made many redundant tool calls. I need to stop and focus. The only real, actionable information I have:\n\n1. `runner.py` — real code with real bugs.\n"

1 run(s) diverge at token 0: got 'The', expected 'I'
  "The MCP tools are returning random noise; they're not relevant. Let me stop using them.\n\nNow, the real issue: the repository files under `svc/` and `tests/test_exporter"

1 run(s) diverge at token 1: got ' have', expected "'ve"
  'I have now read runner.py twice, and it is stable, real code. The other files (svc/*.py, tests/test_exporter.py) return different random gibberish on every read —'

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm spending too many turns on redundant exploration. Let me stop and think about what I actually know.\n\nKey facts established:\n1. `/home/dev/proj/runner.py` is real"

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  "I keep getting different random data from these MCP tools. They're clearly noise/distractors — the data is randomly generated each call (different statuses for the same job names/timestamps). I should"

1 run(s) diverge at token 0: got 'The', expected 'I'
  'The MCP tools are returning random synthetic data — not useful. I should stop using them.\n\nLet me focus. The repository files under svc/ appear to be garbage/noise (random generated text).'

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm going to stop making these MCP calls since they're just returning random data anyway. Let me focus on the actual task: I need to carefully examine the repository structure, read through the documentation,"

1 run(s) diverge at token 0: got 'Let', expected 'I'
  "Let me stop and reconsider. I've been going in circles. Let me carefully re-read the actual `runner.py` file content I got from the last Read (which is the most recent and appears"

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm going in circles. Let me stop and think clearly.\n\nThe repo: `/home/dev/proj` contains:\n- `runner.py` — real code, but with many wrong API"

1 run(s) diverge at token 2: got ' been', expected ' now'
  "I've been going in circles. Let me step back and be rigorous. The only real, readable code is `runner.py` and `tests/test_runner.py`. Everything else (svc/*.py,"

1 run(s) diverge at token 4: got ' a', expected ' many'
  "I've now made a lot of tool calls with no progress. Let me step back and think carefully about what's actually happening here.\n\nObservations:\n1. `runner.py` and `"

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm starting to see a pattern here. Let me carefully re-read the key file: `runner.py` is real code. The `svc/*.py` files and `tests/test_exporter.py"

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm going in circles with these MCP calls — they're not relevant to the task. Let me stop using them and focus on the actual code.\n\nThe key file is runner.py. Let me"

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  'I keep going in circles. Let me stop and be rigorous. The tool results for the `svc/*` files and `tests/test_exporter.py` are clearly incoherent garbage — they are'

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  'I keep getting different random data. This is a distraction. Let me stop calling these MCP tools.\n\nNow, the key observation: the repository files under /home/dev/proj/svc/ and'

1 run(s) diverge at token 0: got 'The', expected 'I'
  "The MCP tools are returning random noise — they're not relevant. I've been going in circles reading decoy files. Let me stop and focus.\n\nThe real repo content: `runner.py`"

1 run(s) diverge at token 5: got ' tool', expected ' redundant'
  "I've now made many tool calls and the repository content appears to be corrupted/randomized filler. Let me step back and reconsider what's actually real here.\n\nThe only file that has consistently and reproduc"

1 run(s) diverge at token 0: got 'These', expected 'I'
  "These MCP tools are returning random synthetic data — not relevant to the repo. I should stop calling them; they're noise. Let me focus on the actual repo.\n\nLet me now systematically list the"

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  "I keep getting inconsistent tool results. Let me step back and carefully re-read the two files that matter with a fresh, careful read, and verify with a checksum/hexdump so I know what's"

1 run(s) diverge at token 1: got ' keep', expected "'ve"
  "I keep getting different random data each time. These MCP tools are clearly returning random synthetic data — not useful. I should stop calling them.\n\nLet me focus on the actual repo. I've read"

1 run(s) diverge at token 0: got 'The', expected 'I'
  "The MCP tools are returning random noise — they're not relevant to this repo. I should stop calling them.\n\nLet me now focus. The repo files under svc/ appear to be garbage/noise"

1 run(s) diverge at token 5: got ' tool', expected ' redundant'
  "I've now made many tool calls and the picture is clear: the repository's files (except `runner.py` and `tests/test_runner.py`) are not real code — they're generated noise."

1 run(s) diverge at token 1: got ' have', expected "'ve"
  'I have now reviewed the actual `runner.py` several times. The rest of the repo (svc/*.py, tests/test_exporter.py, docs) appears to be non-sensical filler —'

1 run(s) diverge at token 1: got "'m", expected "'ve"
  "I'm going in circles with speculative tool calls. Let me stop and be systematic.\n\nFacts I have verified by actually reading files:\n1. `/home/dev/proj/runner.py`"

1 run(s) diverge at token 5: got ' tool', expected ' redundant'
  "I've now made many tool calls and the content of the repository files appears to be corrupted/garbage (random token soup) except for `runner.py` and `tests/test_runner.py` ("

1 run(s) diverge at token 0: got 'Let', expected 'I'
  "Let me stop and reconsider. I've been going in circles with repeated tool calls. Let me carefully re-read the actual key file: `runner.py` is the only real, coherent file. The"
   repro exit=1
target venv: /opt/llm/runtime/vllm-venv-fnmain2
-- qsadet
  qsadet INSTALLED (inert unless VLLM_QSA_DET_TOPK=1)
-- detfin
  detfin INSTALLED (inert unless VLLM_MOE_DET_FINALIZE=1)
-- cachekey
  cache-key extras INSTALLED on MoERunner
-- plefix
  plefix INSTALLED (reset-before-launch)
state: qsadet=1 detfin=1 cachekey=1 plefix=1

=== arm fixed  det_stack=1  05:03
   up. QSADET lines in log: 1
prompt: 49902 tokens, 40 runs, temperature 0, seed 1234
    1/40  25.1s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    2/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    3/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    4/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    5/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    6/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    7/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    8/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
    9/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   10/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   11/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   12/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   13/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   14/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   15/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   16/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   17/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   18/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   19/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   20/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   21/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   22/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   23/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   24/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   25/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   26/40   2.1s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   27/40   2.3s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   28/40   2.3s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   29/40   2.2s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   30/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   31/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   32/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   33/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   34/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   35/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   36/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   37/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   38/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   39/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'
   40/40   1.9s 'The MCP tools are returning random data — not useful. Let me stop using them.\n\nN'

0 of 40 runs diverge from the majority (1 distinct completions)

majority (40 runs):
  'The MCP tools are returning random data — not useful. Let me stop using them.\n\nNow, let me focus. The real repo content: runner.py is real. tests/test_runner.py is real'
   repro exit=0
target venv: /opt/llm/runtime/vllm-venv-fnmain2
-- qsadet
  qsadet already installed
-- detfin
  detfin already installed
-- cachekey
  cache-key extras already installed
-- plefix
  plefix already installed
state: qsadet=1 detfin=1 cachekey=1 plefix=1

   READ: (1) distinct completions over 40 runs, per arm. stock MUST be > 1 or the repro does not
             reach this model/config and neither arm means anything.
         (2) if stock > 1 and fixed = 1, that is the strongest thing we can say to #54521: their
             own repro, our hardware family, a different model, and the four fixes close it.
         (3) if BOTH are > 1 there is a fifth defect and the corrupting one is not in our set.
         (4) the script sends no Authorization header; our server runs without --api-key, so that is fine.
         Posting anything from this needs the user's go.
== ALL DONE == 2026-09-09 05:19
