detplug: persistent_topk -> _C_det.persistent_topk
..............................................................ssssssssss [ 18%]
sss.............FFFFFFFFFFFFFFF............FFFFFFFFssss.........FFFsFFFs [ 37%]
F.F..F..F..FF..............FFFFFFFFFFFFFF...FF...............sss........ [ 56%]
....sss...........................sss............sss..................ss [ 75%]
sssssss............sss.................................................. [ 93%]
.............s.s.......                                                  [100%]
=================================== FAILURES ===================================
_______ test_deepseek_workspace_topk[cooperative_topk-1-2048-True-short] _______

seq_len_range = (4000, 8000), test_id = 'short_sequences', clean_logits = True
top_k = 2048, next_n = 1, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...type=torch.int32), tensor([ 15,  29,   1,  ...,   0, 144,  63], device='cuda:0',
       dtype=torch.uint8), 2048, 7252)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_deepseek_workspace_topk[cooperative_topk-1-2048-True-medium] _______

seq_len_range = (8000, 32000), test_id = 'medium_sequences', clean_logits = True
top_k = 2048, next_n = 1, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 30904)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_deepseek_workspace_topk[cooperative_topk-1-2048-True-long] ________

seq_len_range = (32000, 163840), test_id = 'long_sequences', clean_logits = True
top_k = 2048, next_n = 1, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 123992)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_deepseek_workspace_topk[cooperative_topk-1-2048-False-short] _______

seq_len_range = (4000, 8000), test_id = 'short_sequences', clean_logits = False
top_k = 2048, next_n = 1, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 7252)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_deepseek_workspace_topk[cooperative_topk-1-2048-False-medium] ______

seq_len_range = (8000, 32000), test_id = 'medium_sequences'
clean_logits = False, top_k = 2048, next_n = 1, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 30904)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_deepseek_workspace_topk[cooperative_topk-1-2048-False-long] _______

seq_len_range = (32000, 163840), test_id = 'long_sequences'
clean_logits = False, top_k = 2048, next_n = 1, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...evice='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 123992)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_deepseek_workspace_topk[cooperative_topk-4-2048-True-short] _______

seq_len_range = (4000, 8000), test_id = 'short_sequences', clean_logits = True
top_k = 2048, next_n = 4, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 7252)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_deepseek_workspace_topk[cooperative_topk-4-2048-True-medium] _______

seq_len_range = (8000, 32000), test_id = 'medium_sequences', clean_logits = True
top_k = 2048, next_n = 4, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 30904)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_deepseek_workspace_topk[cooperative_topk-4-2048-True-long] ________

seq_len_range = (32000, 163840), test_id = 'long_sequences', clean_logits = True
top_k = 2048, next_n = 4, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...evice='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 123992)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_deepseek_workspace_topk[cooperative_topk-4-2048-False-short] _______

seq_len_range = (4000, 8000), test_id = 'short_sequences', clean_logits = False
top_k = 2048, next_n = 4, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 7252)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_deepseek_workspace_topk[cooperative_topk-4-2048-False-medium] ______

seq_len_range = (8000, 32000), test_id = 'medium_sequences'
clean_logits = False, top_k = 2048, next_n = 4, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 30904)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_deepseek_workspace_topk[cooperative_topk-4-2048-False-long] _______

seq_len_range = (32000, 163840), test_id = 'long_sequences'
clean_logits = False, top_k = 2048, next_n = 4, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "seq_len_range,test_id",
        [
            pytest.param((4000, 8000), "short_sequences", id="short"),
            pytest.param((8000, 32000), "medium_sequences", id="medium"),
            pytest.param((32000, 163840), "long_sequences", id="long"),
        ],
    )
    @pytest.mark.parametrize("clean_logits", [True, False])
    @pytest.mark.parametrize("top_k", [2048])
    @pytest.mark.parametrize("next_n", [1, 4])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_deepseek_workspace_topk(
        seq_len_range: tuple[int, int],
        test_id: str,
        clean_logits: bool,
        top_k: int,
        next_n: int,
        backend: str,
    ) -> None:
        """
        Test workspace top-k backends with varying sequence lengths and speculative
        decoding.
        Supports speculative decoding with next_n > 1.
        """
        set_random_seed(42 if test_id == "short_sequences" else 43)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        num_rows = batch_size * next_n
    
        seq_lens = torch.randint(
            seq_len_range[0],
            seq_len_range[1],
            (batch_size,),
            dtype=torch.int32,
            device="cuda",
        )
        seq_lens = (seq_lens + 3) & ~3  # align to 4 for TMA
    
        # Compute row boundaries for speculative decoding
        row_starts = torch.zeros(num_rows, dtype=torch.int32, device="cuda")
        row_indices = torch.arange(num_rows, device="cuda") // next_n
        next_n_offset = torch.arange(num_rows, device="cuda") % next_n
        row_ends = seq_lens[row_indices] - next_n + next_n_offset + 1
    
        logits = create_random_logits(
            row_starts, row_ends, torch.float32, 42, clean_logits, "random"
        )
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
    
        if next_n == 1:
            lengths = seq_lens
        else:
            offsets = torch.arange(next_n, device=logits.device, dtype=torch.int32)
            lengths = (seq_lens.unsqueeze(1) - next_n + 1 + offsets).flatten()
    
        max_seq_len = int(seq_lens.max().item())
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len, next_n)

/opt/llm/runners/test_tk.py:731: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...evice='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 123992)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
________________________ test_cooperative_topk_cs2[512] ________________________

top_k = 512

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("top_k", [512, 1024, 2048])
    def test_cooperative_topk_cs2(top_k: int) -> None:
        """The 64-row dispatch uses the two-CTA cooperative kernel."""
>       run_large_context_topk_test(
            batch_size=64,
            seq_lens=[65536] * 64,
            top_k=top_k,
            backend="cooperative_topk",
        )

/opt/llm/runners/test_tk.py:888: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 65536)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______________________ test_cooperative_topk_cs2[1024] ________________________

top_k = 1024

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("top_k", [512, 1024, 2048])
    def test_cooperative_topk_cs2(top_k: int) -> None:
        """The 64-row dispatch uses the two-CTA cooperative kernel."""
>       run_large_context_topk_test(
            batch_size=64,
            seq_lens=[65536] * 64,
            top_k=top_k,
            backend="cooperative_topk",
        )

/opt/llm/runners/test_tk.py:888: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 1024, 65536)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______________________ test_cooperative_topk_cs2[2048] ________________________

top_k = 2048

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("top_k", [512, 1024, 2048])
    def test_cooperative_topk_cs2(top_k: int) -> None:
        """The 64-row dispatch uses the two-CTA cooperative kernel."""
>       run_large_context_topk_test(
            batch_size=64,
            seq_lens=[65536] * 64,
            top_k=top_k,
            backend="cooperative_topk",
        )

/opt/llm/runners/test_tk.py:888: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 65536)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_ test_workspace_topk_correctness[cooperative_topk-seq_len_edge_very_small_to_medium] _

test_config = {'seq_lens': [1, 10, 100, 2048], 'top_k': 2048, 'data_type': 'random'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 2048)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
____ test_workspace_topk_correctness[cooperative_topk-seq_len_edge_above_k] ____

test_config = {'seq_lens': [2049, 2100, 2500, 3000], 'top_k': 2048, 'data_type': 'random'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 3000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_ test_workspace_topk_correctness[cooperative_topk-algo_transition_filtered_radix] _

test_config = {'seq_lens': [8000, 16384, 20000], 'top_k': 2048, 'data_type': 'random'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 20000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
___ test_workspace_topk_correctness[cooperative_topk-data_sorted_ascending] ____

test_config = {'seq_lens': [5000, 10000], 'top_k': 2048, 'data_type': 'sorted_asc'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 10000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
___ test_workspace_topk_correctness[cooperative_topk-data_sorted_descending] ___

test_config = {'seq_lens': [5000, 10000], 'top_k': 2048, 'data_type': 'sorted_desc'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 10000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_workspace_topk_correctness[cooperative_topk-data_all_same] ________

test_config = {'seq_lens': [5000, 10000], 'top_k': 2048, 'data_type': 'all_same'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 10000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_______ test_workspace_topk_correctness[cooperative_topk-data_many_ties] _______

test_config = {'seq_lens': [5000, 10000], 'top_k': 2048, 'data_type': 'many_ties'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 10000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
____ test_workspace_topk_correctness[cooperative_topk-data_float_precision] ____

test_config = {'seq_lens': [5000, 10000], 'top_k': 2048, 'data_type': 'small_differences'}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Sequence Length Edge Cases ====================
            pytest.param(
                {"seq_lens": [1, 10, 100, 2048], "top_k": 2048, "data_type": "random"},
                id="seq_len_edge_very_small_to_medium",
            ),
            pytest.param(
                {
                    "seq_lens": [2049, 2100, 2500, 3000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="seq_len_edge_above_k",
            ),
            pytest.param(
                {"seq_lens": [8000, 16384, 20000], "top_k": 2048, "data_type": "random"},
                id="algo_transition_filtered_radix",
            ),
            # ==================== CATEGORY: Data Distributions ====================
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_asc"},
                id="data_sorted_ascending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "sorted_desc"},
                id="data_sorted_descending",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "all_same"},
                id="data_all_same",
            ),
            pytest.param(
                {"seq_lens": [5000, 10000], "top_k": 2048, "data_type": "many_ties"},
                id="data_many_ties",
            ),
            pytest.param(
                {
                    "seq_lens": [5000, 10000],
                    "top_k": 2048,
                    "data_type": "small_differences",
                },
                id="data_float_precision",
            ),
            # ==================== CATEGORY: Alignment / Vectorization ====================
            pytest.param(
                {
                    "seq_lens": [2055, 2056, 2057, 2063],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_vec_boundaries_low",
            ),
            pytest.param(
                {
                    "seq_lens": [4095, 4096, 4097, 4102],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_4k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [8191, 8192, 8193, 8198],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_8k_boundary",
            ),
            pytest.param(
                {
                    "seq_lens": [16383, 16384, 16385, 16390],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="align_16k_boundary",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_correctness(test_config: dict, backend: str) -> None:
        """
        Comprehensive correctness tests covering:
        - Sequence length edge cases (trivial, boundary, varied)
        - Very small sequences (< 100 elements)
        - Mixed sequence lengths in same batch
        - Data distributions (sorted, ties, precision)
        - Memory alignment / vectorization boundaries
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=test_config["top_k"],
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:988: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 1, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 10000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
________ test_workspace_topk_algorithm_paths[cooperative_topk-batch_1] _________

test_config = {'batch_size': 1, 'seq_len': 5000, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...e='cuda:0', dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 2048, 5000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
________ test_workspace_topk_algorithm_paths[cooperative_topk-batch_4] _________

test_config = {'batch_size': 4, 'seq_len': 5000, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...type=torch.int32), tensor([158,  15,   0,  ...,   0,   0,   0], device='cuda:0',
       dtype=torch.uint8), 2048, 5000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
________ test_workspace_topk_algorithm_paths[cooperative_topk-batch_32] ________

test_config = {'batch_size': 32, 'seq_len': 5000, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...:0',
       dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 2048, 5000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_____ test_workspace_topk_algorithm_paths[cooperative_topk-single_cta_4k] ______

test_config = {'batch_size': 2, 'seq_len': 4096, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 4096)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_____ test_workspace_topk_algorithm_paths[cooperative_topk-single_cta_8k] ______

test_config = {'batch_size': 2, 'seq_len': 8192, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 8192)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_ test_workspace_topk_algorithm_paths[cooperative_topk-multi_cta_163840_dsv3_max] _

test_config = {'batch_size': 2, 'seq_len': 163840, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...pe=torch.int32), tensor([221, 172,  70,  ...,   0,   0,   0], device='cuda:0',
       dtype=torch.uint8), 2048, 163840)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_ test_workspace_topk_algorithm_paths[cooperative_topk-extreme_dsv3_max_context] _

test_config = {'batch_size': 2, 'seq_len': 163840, 'top_k': 2048}
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # ==================== CATEGORY: Batch Size Scalability ====================
            pytest.param(
                {"batch_size": 1, "seq_len": 5000, "top_k": 2048},
                id="batch_1",
            ),
            pytest.param(
                {"batch_size": 4, "seq_len": 5000, "top_k": 2048},
                id="batch_4",
            ),
            pytest.param(
                {"batch_size": 32, "seq_len": 5000, "top_k": 2048},
                id="batch_32",
            ),
            pytest.param(
                {"batch_size": 256, "seq_len": 5000, "top_k": 2048},
                id="batch_256",
            ),
            # ==================== CATEGORY: Single-CTA vs Multi-CTA ====================
            pytest.param(
                {"batch_size": 2, "seq_len": 4096, "top_k": 2048},
                id="single_cta_4k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 8192, "top_k": 2048},
                id="single_cta_8k",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="multi_cta_163840_dsv3_max",
            ),
            # ==================== CATEGORY: Extreme Cases ====================
            pytest.param(
                {"batch_size": 512, "seq_len": 5000, "top_k": 2048},
                id="extreme_large_batch",
            ),
            pytest.param(
                {"batch_size": 2, "seq_len": 163840, "top_k": 2048},
                id="extreme_dsv3_max_context",
            ),
        ],
    )
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_algorithm_paths(test_config: dict, backend: str) -> None:
        """
        Test different algorithm execution paths (capped at 163840 for DeepSeek V3.2):
        - Batch size scalability (1, 4, 32, 256)
        - Single-CTA vs Multi-CTA execution
        - Extreme configurations (large batch, max context length)
        """
>       run_large_context_topk_test(
            batch_size=test_config["batch_size"],
            seq_lens=[test_config["seq_len"]] * test_config["batch_size"],
            top_k=test_config["top_k"],
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1051: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0', dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 2048, 163840)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_________________ test_workspace_topk_stress[cooperative_topk] _________________

backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_stress(backend: str) -> None:
        """
        Stress test with random configurations to catch edge cases.
        Capped at 163840 (DeepSeek V3.2 max context) for realistic testing.
        """
        torch.set_default_device("cuda:0")
        top_k = 2048
    
        for seed in range(3):
            set_random_seed(seed)
    
            # Random batch size (limited for speed)
            batch_size = torch.randint(1, 32, (1,)).item()
    
            # Random sequence lengths capped at DeepSeek V3.2 max context
            seq_lens_tensor = torch.randint(100, 163840, (batch_size,))
            if backend == "cooperative_topk":
                seq_lens = ((seq_lens_tensor + 3) & ~3).tolist()
            else:
                seq_lens = seq_lens_tensor.tolist()
    
>           run_large_context_topk_test(
                batch_size=batch_size,
                seq_lens=seq_lens,
                top_k=top_k,
                seed=seed,
                backend=backend,
            )

/opt/llm/runners/test_tk.py:1083: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...evice='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 163676)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
___ test_deepseek_topk_backends_no_error_and_reference[512-cooperative_topk] ___

backend = 'cooperative_topk', top_k = 512

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("backend", TOPK_BACKENDS)
    @pytest.mark.parametrize("top_k", [512, 1024, 2048])
    @torch.inference_mode()
    def test_deepseek_topk_backends_no_error_and_reference(
        backend: str,
        top_k: int,
    ) -> None:
        """Exercise every production top-k backend on the same inputs."""
>       run_large_context_topk_test(
            batch_size=4,
            seq_lens=[2049, 4097, 8191, 12000],
            top_k=top_k,
            data_type="random",
            seed=123,
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 12000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
__ test_deepseek_topk_backends_no_error_and_reference[1024-cooperative_topk] ___

backend = 'cooperative_topk', top_k = 1024

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("backend", TOPK_BACKENDS)
    @pytest.mark.parametrize("top_k", [512, 1024, 2048])
    @torch.inference_mode()
    def test_deepseek_topk_backends_no_error_and_reference(
        backend: str,
        top_k: int,
    ) -> None:
        """Exercise every production top-k backend on the same inputs."""
>       run_large_context_topk_test(
            batch_size=4,
            seq_lens=[2049, 4097, 8191, 12000],
            top_k=top_k,
            data_type="random",
            seed=123,
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 1024, 12000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
__ test_deepseek_topk_backends_no_error_and_reference[2048-cooperative_topk] ___

backend = 'cooperative_topk', top_k = 2048

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("backend", TOPK_BACKENDS)
    @pytest.mark.parametrize("top_k", [512, 1024, 2048])
    @torch.inference_mode()
    def test_deepseek_topk_backends_no_error_and_reference(
        backend: str,
        top_k: int,
    ) -> None:
        """Exercise every production top-k backend on the same inputs."""
>       run_large_context_topk_test(
            batch_size=4,
            seq_lens=[2049, 4097, 8191, 12000],
            top_k=top_k,
            data_type="random",
            seed=123,
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1101: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...0',
       dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 2048, 12000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______________ test_cooperative_topk_512_tie_workspace_is_per_row ______________

    @pytest.mark.skipif(not _has_device_capability(90), reason="This test requires SM90+")
    @torch.inference_mode()
    def test_cooperative_topk_512_tie_workspace_is_per_row() -> None:
        """Regression test for TopK=512 tie workspace row overlap."""
        torch.set_default_device("cuda:0")
    
        top_k = 512
        num_rows = 2
        stride = 65536
        lengths = torch.tensor([40960, 65536], dtype=torch.int32, device="cuda")
        logits = torch.full(
            (num_rows, stride), float("-inf"), dtype=torch.float32, device="cuda"
        )
    
        # Row 0 must never select these low indices: many better row-0 ties exist.
        logits[0, :2048] = -10.0
        logits[0, 2048 : lengths[0]] = 1.0
        # Row 1 has higher exact tie scores. With the old row * TopK tie_ws stride,
        # these row-1 ties could overwrite row 0's TopK=512 refinement workspace.
        logits[1, : lengths[1]] = 2.0
    
        indices = torch.empty((num_rows, top_k), dtype=torch.int32, device="cuda")
        workspace = torch.empty(RADIX_TOPK_WORKSPACE_SIZE, dtype=torch.uint8, device="cuda")
>       torch.ops._C.cooperative_topk(logits, lengths, indices, workspace, top_k, stride)

/opt/llm/runners/test_tk.py:1134: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 65536)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
__________ test_workspace_topk[cooperative_topk-512-mixed_all_paths] ___________

test_config = {'seq_lens': [2000, 6000, 30000, 80000], 'data_type': 'random'}
top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 80000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_________ test_workspace_topk[cooperative_topk-512-all_decode_medium] __________

test_config = {'seq_lens': [2048, 4096, 8192, 16000], 'data_type': 'random'}
top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 16000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_____________ test_workspace_topk[cooperative_topk-512-all_large] ______________

test_config = {'seq_lens': [70000, 100000, 163840], 'data_type': 'random'}
top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0', dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 512, 163840)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_workspace_topk[cooperative_topk-512-large_threshold_boundary] ______

test_config = {'seq_lens': [32767, 32768, 32769, 32772], 'data_type': 'random'}
top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 32772)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_________ test_workspace_topk[cooperative_topk-512-single_row_medium] __________

test_config = {'seq_lens': [5000], 'data_type': 'random'}, top_k = 512
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/..., device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 5000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
__________ test_workspace_topk[cooperative_topk-512-single_row_large] __________

test_config = {'seq_lens': [100000], 'top_k': 2048, 'data_type': 'random'}
top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 512, 100000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
______ test_workspace_topk[cooperative_topk-512-trivial_medium_large_mix] ______

test_config = {'seq_lens': [100, 2048, 10000, 80000], 'data_type': 'random'}
top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...e='cuda:0', dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 512, 80000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
__________ test_workspace_topk[cooperative_topk-2048-mixed_all_paths] __________

test_config = {'seq_lens': [2000, 6000, 30000, 80000], 'data_type': 'random'}
top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 80000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_________ test_workspace_topk[cooperative_topk-2048-all_decode_medium] _________

test_config = {'seq_lens': [2048, 4096, 8192, 16000], 'data_type': 'random'}
top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 16000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_____________ test_workspace_topk[cooperative_topk-2048-all_large] _____________

test_config = {'seq_lens': [70000, 100000, 163840], 'data_type': 'random'}
top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 163840)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_____ test_workspace_topk[cooperative_topk-2048-large_threshold_boundary] ______

test_config = {'seq_lens': [32767, 32768, 32769, 32772], 'data_type': 'random'}
top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...='cuda:0', dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 2048, 32772)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_________ test_workspace_topk[cooperative_topk-2048-single_row_medium] _________

test_config = {'seq_lens': [5000], 'data_type': 'random'}, top_k = 2048
backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/... device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 5000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_________ test_workspace_topk[cooperative_topk-2048-single_row_large] __________

test_config = {'seq_lens': [100000], 'top_k': 2048, 'data_type': 'random'}
top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 100000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
_____ test_workspace_topk[cooperative_topk-2048-trivial_medium_large_mix] ______

test_config = {'seq_lens': [100, 2048, 10000, 80000], 'data_type': 'random'}
top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize(
        "test_config",
        [
            # Mixed batch: rows spanning all four paths (trivial, decode, medium, large)
            pytest.param(
                {
                    "seq_lens": [2000, 6000, 30000, 80000],
                    "data_type": "random",
                },
                id="mixed_all_paths",
            ),
            # All decode/medium rows (typical decode scenario)
            pytest.param(
                {
                    "seq_lens": [2048, 4096, 8192, 16000],
                    "data_type": "random",
                },
                id="all_decode_medium",
            ),
            # All large rows
            pytest.param(
                {
                    "seq_lens": [70000, 100000, 163840],
                    "data_type": "random",
                },
                id="all_large",
            ),
            # Boundary around LARGE_THRESHOLD (32K)
            pytest.param(
                {
                    "seq_lens": [32767, 32768, 32769, 32772],
                    "data_type": "random",
                },
                id="large_threshold_boundary",
            ),
            # Single row medium
            pytest.param(
                {
                    "seq_lens": [5000],
                    "data_type": "random",
                },
                id="single_row_medium",
            ),
            # Single row large
            pytest.param(
                {
                    "seq_lens": [100000],
                    "top_k": 2048,
                    "data_type": "random",
                },
                id="single_row_large",
            ),
            # Trivial rows mixed with medium and large
            pytest.param(
                {
                    "seq_lens": [100, 2048, 10000, 80000],
                    "data_type": "random",
                },
                id="trivial_medium_large_mix",
            ),
        ],
    )
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk(test_config: dict, top_k: int, backend: str) -> None:
        """
        Tests specific to workspace top-k backends:
        - Mixed medium/large rows in the same batch (dynamic per-row dispatch)
        - Boundary around LARGE_THRESHOLD (32K)
        - Trivial + medium + large rows in a single batch
        """
>       run_large_context_topk_test(
            batch_size=len(test_config["seq_lens"]),
            seq_lens=test_config["seq_lens"],
            top_k=top_k,
            data_type=test_config.get("data_type", "random"),
            backend=backend,
        )

/opt/llm/runners/test_tk.py:1217: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:813: in run_large_context_topk_test
    _run_topk_backend(backend, logits, lengths, indices, top_k, max_seq_len)
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...'cuda:0',
       dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 80000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
___________ test_workspace_topk_padded_stride[cooperative_topk-512] ____________

top_k = 512, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_padded_stride(top_k: int, backend: str) -> None:
        """
        Test workspace top-k backends with padded logits (large stride, small seq_len)
        to simulate the e2e CUDAGraph scenario where fp8_paged_mqa_logits
        returns [B, max_model_len] with max_model_len=163840.
        """
        set_random_seed(42)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        padded_stride = 163840  # DeepSeek-V3.2 max_model_len
        actual_seq_lens = [3000, 5000, 8000, 12000]
    
        # Create padded logits tensor (like fp8_paged_mqa_logits output)
        logits = torch.full(
            (batch_size, padded_stride),
            float("-inf"),
            dtype=torch.float32,
            device="cuda",
        )
        for i, sl in enumerate(actual_seq_lens):
            logits[i, :sl] = torch.randn(sl, dtype=torch.float32, device="cuda")
    
        lengths = torch.tensor(actual_seq_lens, dtype=torch.int32, device="cuda")
        indices = torch.empty((batch_size, top_k), dtype=torch.int32, device="cuda")
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max(actual_seq_lens))

/opt/llm/runners/test_tk.py:1297: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...e='cuda:0', dtype=torch.int32), tensor([ 0,  0,  0,  ..., 15,  0,  0], device='cuda:0', dtype=torch.uint8), 512, 12000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
___________ test_workspace_topk_padded_stride[cooperative_topk-2048] ___________

top_k = 2048, backend = 'cooperative_topk'

    @pytest.mark.skipif(not current_platform.is_cuda(), reason="This test requires CUDA")
    @pytest.mark.parametrize("top_k", [512, 2048])
    @pytest.mark.parametrize("backend", WORKSPACE_TOPK_BACKENDS)
    @torch.inference_mode()
    def test_workspace_topk_padded_stride(top_k: int, backend: str) -> None:
        """
        Test workspace top-k backends with padded logits (large stride, small seq_len)
        to simulate the e2e CUDAGraph scenario where fp8_paged_mqa_logits
        returns [B, max_model_len] with max_model_len=163840.
        """
        set_random_seed(42)
        torch.set_default_device("cuda:0")
    
        batch_size = 4
        padded_stride = 163840  # DeepSeek-V3.2 max_model_len
        actual_seq_lens = [3000, 5000, 8000, 12000]
    
        # Create padded logits tensor (like fp8_paged_mqa_logits output)
        logits = torch.full(
            (batch_size, padded_stride),
            float("-inf"),
            dtype=torch.float32,
            device="cuda",
        )
        for i, sl in enumerate(actual_seq_lens):
            logits[i, :sl] = torch.randn(sl, dtype=torch.float32, device="cuda")
    
        lengths = torch.tensor(actual_seq_lens, dtype=torch.int32, device="cuda")
        indices = torch.empty((batch_size, top_k), dtype=torch.int32, device="cuda")
>       _run_topk_backend(backend, logits, lengths, indices, top_k, max(actual_seq_lens))

/opt/llm/runners/test_tk.py:1297: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/opt/llm/runners/test_tk.py:76: in _run_topk_backend
    torch.ops._C.cooperative_topk(
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: in __call__
    return self._op(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^
/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/utils/_device.py:122: in __torch_function__
    return func(*args, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <OpOverloadPacket(op='_C.cooperative_topk')>
args = (<[AcceleratorError("CUDA error: invalid argument\nSearch for `cudaErrorInvalidValue' in https://docs.nvidia.com/cuda/...device='cuda:0', dtype=torch.int32), tensor([0, 0, 0,  ..., 0, 0, 0], device='cuda:0', dtype=torch.uint8), 2048, 12000)
kwargs = {}

    def __call__(self, /, *args: _P.args, **kwargs: _P.kwargs) -> _T:
        # overloading __call__ to ensure torch.ops.foo.bar()
        # is still callable from JIT
        # We save the function ptr as the `op` attribute on
        # OpOverloadPacket to access it here.
    
        # Directly calling OverloadPacket goes into C++, which will check
        # the schema and cause an error for torchbind op when inputs consist of FakeScriptObject so we
        # intercept it here and call TorchBindOpverload instead.
        if self._has_torchbind_op_overload and _must_dispatch_in_python(args, kwargs):
            # pyrefly: ignore [bad-argument-type]
            return _call_overload_packet_from_python(self, *args, **kwargs)
>       return self._op(*args, **kwargs)
               ^^^^^^^^^^^^^^^^^^^^^^^^^
E       RuntimeError: launch_cooperative_cluster, /workspace/csrc/libtorch_stable/cooperative_topk.cu:48, cooperative_topk launch failed: invalid argument

/opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/_ops.py:1279: RuntimeError
=============================== warnings summary ===============================
test_tk.py: 14 warnings
  /opt/llm/runtime/vllm-venv-fnmain2/lib/python3.12/site-packages/torch/jit/_script.py:365: DeprecationWarning: `torch.jit.script_method` is deprecated. Please switch to `torch.compile` or `torch.export`.
    warnings.warn(

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-1-2048-True-short]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-1-2048-True-medium]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-1-2048-True-long]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-1-2048-False-short]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-1-2048-False-medium]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-1-2048-False-long]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-4-2048-True-short]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-4-2048-True-medium]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-4-2048-True-long]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-4-2048-False-short]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-4-2048-False-medium]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_workspace_topk[cooperative_topk-4-2048-False-long]
FAILED ../opt/llm/runners/test_tk.py::test_cooperative_topk_cs2[512] - Runtim...
FAILED ../opt/llm/runners/test_tk.py::test_cooperative_topk_cs2[1024] - Runti...
FAILED ../opt/llm/runners/test_tk.py::test_cooperative_topk_cs2[2048] - Runti...
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-seq_len_edge_very_small_to_medium]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-seq_len_edge_above_k]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-algo_transition_filtered_radix]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-data_sorted_ascending]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-data_sorted_descending]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-data_all_same]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-data_many_ties]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_correctness[cooperative_topk-data_float_precision]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-batch_1]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-batch_4]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-batch_32]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-single_cta_4k]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-single_cta_8k]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-multi_cta_163840_dsv3_max]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_algorithm_paths[cooperative_topk-extreme_dsv3_max_context]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_stress[cooperative_topk]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_topk_backends_no_error_and_reference[512-cooperative_topk]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_topk_backends_no_error_and_reference[1024-cooperative_topk]
FAILED ../opt/llm/runners/test_tk.py::test_deepseek_topk_backends_no_error_and_reference[2048-cooperative_topk]
FAILED ../opt/llm/runners/test_tk.py::test_cooperative_topk_512_tie_workspace_is_per_row
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-mixed_all_paths]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-all_decode_medium]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-all_large]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-large_threshold_boundary]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-single_row_medium]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-single_row_large]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-512-trivial_medium_large_mix]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-mixed_all_paths]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-all_decode_medium]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-all_large]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-large_threshold_boundary]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-single_row_medium]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-single_row_large]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk[cooperative_topk-2048-trivial_medium_large_mix]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_padded_stride[cooperative_topk-512]
FAILED ../opt/llm/runners/test_tk.py::test_workspace_topk_padded_stride[cooperative_topk-2048]
51 failed, 287 passed, 45 skipped, 14 warnings in 356.41s (0:05:56)
== ALL DONE == 14:37
