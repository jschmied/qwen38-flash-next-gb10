diff --git a/csrc/libtorch_stable/persistent_topk.cuh b/csrc/libtorch_stable/persistent_topk.cuh
index 79426b5f..95187c63 100644
--- a/csrc/libtorch_stable/persistent_topk.cuh
+++ b/csrc/libtorch_stable/persistent_topk.cuh
@@ -170,6 +170,27 @@ __device__ void det_select_row(const float* __restrict__ row, int n,
       reinterpret_cast<uint32_t*>(reinterpret_cast<char*>(smem) + fixed);
   const bool cached =
       (fixed + static_cast<size_t>(n) * sizeof(uint32_t) <= smem_bytes);
+  // After the first pass only the threshold bin's keys can still change the
+  // answer -- about n/256 of the row on random data. Pass 1 already reads the
+  // whole row and filters to exactly that set, so appending it to a survivor
+  // buffer there costs no extra traffic, and passes 2 and 3 then read the
+  // buffer instead of rescanning the row. That is what turns the uncached case
+  // from four passes over global memory into two.
+  //
+  // Compaction ORDER is deliberately unconstrained: survivors are only ever
+  // histogrammed, and a histogram is order-independent, so the atomic offset
+  // cannot make the result depend on thread arrival. The decision to compact at
+  // all depends only on the bin population and the buffer capacity, both of
+  // which are functions of the input shape -- never of scheduling.
+  const size_t surv_off =
+      fixed + (cached ? static_cast<size_t>(n) * sizeof(uint32_t) : size_t{0});
+  uint32_t* surv =
+      reinterpret_cast<uint32_t*>(reinterpret_cast<char*>(smem) + surv_off);
+  const uint32_t surv_cap = static_cast<uint32_t>(
+      smem_bytes > surv_off ? (smem_bytes - surv_off) / sizeof(uint32_t) : 0u);
+  bool compact = false;   // pass 1 should append survivors
+  bool compacted = false; // the buffer is populated; passes 2+ may use it
+  uint32_t surv_n = 0;
   const int tx = threadIdx.x;
   uint32_t prefix = 0;
   uint32_t remaining = TopK;
@@ -212,6 +233,21 @@ __device__ void det_select_row(const float* __restrict__ row, int n,
         if (cached) keys[i] = k;
         atomicAdd(&hist[k >> 24], 1);
       }
+    } else if (compacted) {
+      // Every survivor already matches pass 1's mask; later passes narrow it.
+      for (int i = tx; i < static_cast<int>(surv_n); i += N_THREADS) {
+        const uint32_t key = surv[i];
+        if ((key & hi_mask) == prefix)
+          atomicAdd(&hist[(key >> shift) & 0xFF], 1);
+      }
+    } else if (compact) {
+      for (int i = tx; i < n; i += N_THREADS) {
+        const uint32_t key = cached ? keys[i] : convert_to_uint32_v2(row[i]);
+        if ((key & hi_mask) == prefix) {
+          atomicAdd(&hist[(key >> shift) & 0xFF], 1);
+          surv[atomicAdd(&hist2[3], 1u)] = key;
+        }
+      }
     } else {
       for (int i = tx; i < n; i += N_THREADS) {
         const uint32_t key = cached ? keys[i] : convert_to_uint32_v2(row[i]);
@@ -269,6 +305,23 @@ __device__ void det_select_row(const float* __restrict__ row, int n,
       __syncthreads();
       break;
     }
+    if (pass == 0) {
+      // bin_pop is exactly how many keys pass 1 will append. Arm compaction
+      // only when the buffer provably cannot overflow; otherwise every pass
+      // keeps rescanning, which is correct, just slower. An all-equal row is
+      // the case that takes this fallback.
+      // Only when the row is NOT cached. Compaction trades a shared-memory
+      // write per survivor in pass 1 against re-reading the row in passes 2-3;
+      // that is a win against GLOBAL re-reads and a loss against shared-memory
+      // ones. Measured on three parts (GB10 sm_121, A100 sm_80, H100 sm_90):
+      // ungated it costs 9-15 % on cached rows and saves 13-17 % on uncached
+      // ones, so the cache flag is the correct switch.
+      compact = !cached && (bin_pop <= surv_cap);
+      if (compact && tx == 0) hist2[3] = 0;
+    } else if (compact && !compacted) {
+      surv_n = hist2[3];
+      compacted = true;
+    }
     __syncthreads();
   }
   const uint32_t pivot = prefix;
@@ -1285,6 +1338,11 @@ struct FilteredTopKTraits<float> {
 constexpr uint32_t FILTERED_TOPK_BLOCK_THREADS = 1024;
 constexpr uint32_t FILTERED_TOPK_SMEM_INPUT_SIZE =
     16 * 1024;  // 16K indices per buffer
+// 128 KB: the size of the two candidate buffers this path used to carry. Those
+// buffers are gone, and the shared memory now caches the row for
+// det_select_row, so the useful size is a function of the row width and the
+// device -- not a constant. Kept as the minimum request so the request never
+// shrinks below what the old code asked for.
 constexpr size_t FILTERED_TOPK_SMEM_DYNAMIC =
     sizeof(int) * 2 * FILTERED_TOPK_SMEM_INPUT_SIZE;  // 128KB
 
@@ -1302,7 +1360,8 @@ __global__ void __launch_bounds__(FILTERED_TOPK_BLOCK_THREADS)
                               IdType* __restrict__ output,
                               const IdType* __restrict__ lengths,
                               uint32_t num_rows, uint32_t top_k,
-                              uint32_t max_len, uint32_t max_seq_len) {
+                              uint32_t max_len, uint32_t max_seq_len,
+                              uint32_t smem_bytes) {
   constexpr uint32_t BLOCK_SIZE = FILTERED_TOPK_BLOCK_THREADS;
 
   const uint32_t bid = blockIdx.x;
@@ -1342,7 +1401,7 @@ __global__ void __launch_bounds__(FILTERED_TOPK_BLOCK_THREADS)
   vllm::persistent::det_select_row<static_cast<int>(MAX_K),
                                    static_cast<int>(BLOCK_SIZE)>(
       reinterpret_cast<const float*>(score), length,
-      reinterpret_cast<int32_t*>(dst), _smem_reg, FILTERED_TOPK_SMEM_DYNAMIC);
+      reinterpret_cast<int32_t*>(dst), _smem_reg, smem_bytes);
 }
 
 // Helper to compute GCD for VEC_SIZE selection
@@ -1372,13 +1431,32 @@ cudaError_t FilteredTopKRaggedTransform(const DType* input,
                                         uint32_t num_rows, uint32_t top_k_val,
                                         uint32_t max_len, uint32_t max_seq_len,
                                         cudaStream_t stream = 0) {
-  constexpr size_t smem_size = FILTERED_TOPK_SMEM_DYNAMIC;
   constexpr int MAX_VEC = 16 / sizeof(DType);
 
+  // det_select_row re-reads the row from GLOBAL memory on each of its four
+  // radix passes unless the row fits in shared memory. The old fixed 128 KB
+  // request caches rows up to ~32K keys; every device that reaches this path
+  // offers more (A100 163 KiB, H100/H200 227 KiB), and asking for it moves the
+  // cutoff to ~41K / ~57K. Ask for what the widest row needs, capped by the
+  // device, floored at the historical request.
+  int device = 0;
+  FLASHINFER_CUDA_CALL(cudaGetDevice(&device));
+  static constexpr int kMaxDevices = 32;
+  static int cached_optin[kMaxDevices] = {};  // 0 = not yet queried
+  int device_optin = (device >= 0 && device < kMaxDevices) ? cached_optin[device] : 0;
+  if (device_optin == 0) {
+    FLASHINFER_CUDA_CALL(cudaDeviceGetAttribute(
+        &device_optin, cudaDevAttrMaxSharedMemoryPerBlockOptin, device));
+    if (device >= 0 && device < kMaxDevices) cached_optin[device] = device_optin;
+  }
+  const uint32_t row_width = max_len < max_seq_len ? max_len : max_seq_len;
+  size_t want = vllm::persistent::det_select_row_bytes<
+      static_cast<int>(MAX_K), static_cast<int>(FILTERED_TOPK_BLOCK_THREADS)>(
+      row_width);
+  if (want < FILTERED_TOPK_SMEM_DYNAMIC) want = FILTERED_TOPK_SMEM_DYNAMIC;
+
   dim3 grid(num_rows);
   dim3 block(FILTERED_TOPK_BLOCK_THREADS);
-  void* args[] = {&input,     &output_indices, &lengths,    &num_rows,
-                  &top_k_val, &max_len,        &max_seq_len};
 
   const int vec_size = ComputeFilteredTopKVecSize<DType>(max_len);
 
@@ -1386,6 +1464,14 @@ cudaError_t FilteredTopKRaggedTransform(const DType* input,
   if (vec_size == VS) {                                                       \
     auto kernel =                                                             \
         FilteredTopKUnifiedKernel<DType, IdType, VS, MAX_K, (VS != MAX_VEC)>; \
+    cudaFuncAttributes fa{};                                                  \
+    FLASHINFER_CUDA_CALL(cudaFuncGetAttributes(&fa, kernel));                 \
+    size_t cap = static_cast<size_t>(device_optin) > fa.sharedSizeBytes       \
+                     ? static_cast<size_t>(device_optin) - fa.sharedSizeBytes \
+                     : 0;                                                     \
+    uint32_t smem_size = static_cast<uint32_t>(want < cap ? want : cap);      \
+    void* args[] = {&input,     &output_indices, &lengths,    &num_rows,      \
+                    &top_k_val, &max_len,        &max_seq_len, &smem_size};   \
     FLASHINFER_CUDA_CALL(cudaFuncSetAttribute(                                \
         kernel, cudaFuncAttributeMaxDynamicSharedMemorySize, smem_size));     \
     FLASHINFER_CUDA_CALL(cudaLaunchKernel((void*)kernel, grid, block, args,   \
